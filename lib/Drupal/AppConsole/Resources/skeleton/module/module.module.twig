<?php

/**
 * @file
 * Contains {{module}}.module
 */

/**
 * Implements hook_help().
 */

function {{module}}_help($path, $arg)
{
  switch ($path) {
    case 'admin/help#{{module}}':
      $output = '{{ description }}';
      return $output;
    break;
  }
}
