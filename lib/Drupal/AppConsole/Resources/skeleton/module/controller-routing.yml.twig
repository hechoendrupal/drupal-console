{% if name is defined %}
{{ module }}.hello:
  path: '/{{module}}/hello/{name}'
  defaults:
    _content: '\Drupal\{{ module }}\Controller\{{ name }}::hello'
    _title: '{{module}} Title'
  requirements:
    _permission: 'access content'
{% endif %}
