{% if name is defined %}
{{ module }}_hello:
  path: '/{{module}}/hello/{name}'
  defaults:
    _content: '\Drupal\{{ module }}\Controller\{{ module }}::helloAction'
    _title: '{{module}} Title'
  requirements:
    _permission: 'access content'
{% endif %}
