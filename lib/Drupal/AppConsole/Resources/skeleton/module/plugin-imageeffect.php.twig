<?php
/**
 *@file
 * Contains \Drupal\{{module}}\Plugin\ImageEffect\{{class.name}}.php
 */

namespace Drupal\{{module}}\Plugin\ImageEffect;

use Drupal\Core\Image\ImageInterface;
use Drupal\image\ImageEffectBase;

/**
 * Provides a '{{class.name}}' image effect.
 *
 * @ImageEffect(
 *  id = "{{plugin_id}}",
 *  label = @Translation("{{plugin_label}}"),
 *  description = @Translation("{{description}}")
 * )
 */
class {{ class.name }} extends ImageEffectBase {

  /**
   * {@inheritdoc}
   */
  public function applyEffect(ImageInterface $image) {
    // Implement Image Effect here
    return imagefilter($image->getToolkit()->getResource());
  }

}
