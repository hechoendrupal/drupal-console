# {{ entity_class }} actions definition
{{ module }}.{{ entity_name }}.add:
  route_name: {{ module }}.{{ entity_name }}.add
  title: 'Add {{ entity_name }}'
  appears_on:
    - {{ module }}.{{ entity_name }}.list
    - {{ module }}.{{ entity_name }}.view
