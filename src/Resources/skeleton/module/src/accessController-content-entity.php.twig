<?php

/**
 * @file
 * Contains \Drupal\account/{{ entity|capitalize }}AccessController
 */

namespace Drupal\{{module}};

use Drupal\Core\Entity\EntityAccessController;
use Drupal\Core\Entity\EntityInterface;
use Drupal\Core\Session\AccountInterface;

/**
 * Access controller for the {{ entity|capitalize }} entity.
 *
 * @see \Drupal\{{module}}\Entity\{{ entity|capitalize }}.
 */
class {{ entity|capitalize }}AccessController extends EntityAccessController {

  /**
   * {@inheritdoc}
   */
  protected function checkAccess(EntityInterface $entity, $operation, $langcode, AccountInterface $account) {

    switch ($operation) {
      case 'view':
        return $account->hasPermission('view {{ entity|capitalize }} entity');
        break;

      case 'edit':
        return $account->hasPermission('edit {{ entity|capitalize }} entity');
        break;

      case 'delete':
        return $account->hasPermission('delete {{ entity|capitalize }} entity');
        break;

    }

    return TRUE;
  }

  /**
   * {@inheritdoc}
   */
  protected function checkCreateAccess(AccountInterface $account, array $context, $entity_bundle = NULL) {
    return $account->hasPermission('add {{ entity|capitalize }} entity');
  }
}
