{% if service_name is defined %}
{% if not file_exists %}
services:
{% endif %}
  {{ service_name | lower }}:
    class: Drupal\{{module}}\{{ class_name }}
    arguments: [{{ servicesAsParametersKeys(services)|join(', ') }}]
{% if tags is defined %}
    tags:
      - { {{ tagsAsArray(tags)|join(', ') }} }
{% endif %}
{% endif %}

