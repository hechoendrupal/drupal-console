name: {{ profile }}
type: {{ type }}
description: {{ description }}
core: {{ core }}
{% if dependencies %}

distribution:
  name: {{ distribution }}
{% endif %}
{% if dependencies %}

dependencies:
{% for dependency in dependencies %}
  - {{ dependency }}
{% endfor %}
{% endif %}
