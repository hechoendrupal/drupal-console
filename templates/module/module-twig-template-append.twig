
/**
 * Implements hook_theme().
 */
function {{machine_name}}_theme() {
  return [
    '{{machine_name}}' => [
      'template' => '{{machine_name}}',
      'render element' => 'children',
    ],
  ];
}
